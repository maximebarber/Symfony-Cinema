<form method="post" action="{{ path('acteur_delete', {'idActeur': acteur.idActeur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acteur.idActeur) }}">
    <button class="btn">Delete</button>
</form>
