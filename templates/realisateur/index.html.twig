{% extends 'base.html.twig' %}

{% block title %}Realisateur index{% endblock %}

{% block body %}
    <h1>Realisateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdRealisateur</th>
                <th>NomRealisateur</th>
                <th>PrenomRealisateur</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for realisateur in realisateurs %}
            <tr>
                <td>{{ realisateur.idRealisateur }}</td>
                <td>{{ realisateur.nomRealisateur }}</td>
                <td>{{ realisateur.prenomRealisateur }}</td>
                <td>
                    <a href="{{ path('realisateur_show', {'idRealisateur': realisateur.idRealisateur}) }}">show</a>
                    <a href="{{ path('realisateur_edit', {'idRealisateur': realisateur.idRealisateur}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('realisateur_new') }}">Create new</a>
{% endblock %}
